import { createStitches } from '@stitches/react';

export const {
    config,
    styled,
    css,
    globalCss,
    keyframes,
    getCssText,
    theme,
    createTheme
} = createStitches({
    theme:{
        colors:{
            primaryColor:"#0B0D17",
            secoundaryColor:"#D0D6F9",
            terciaryColor:"#FFFFFF",
            bgNav:"#979797"
        },
    }
})
